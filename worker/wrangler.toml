name               = "schreinerei-hergert-kontakt"
main               = "worker.js"
compatibility_date = "2024-11-01"

# Standalone workers.dev deployment only.
# This Worker lives at:
#   https://schreinerei-hergert-kontakt.philip-61a.workers.dev
# It has NO routes and does NOT attach to the Pages project.
workers_dev = true

# Deploy:  npx wrangler deploy --config wrangler.toml
# Logs:    npx wrangler tail schreinerei-hergert-kontakt
