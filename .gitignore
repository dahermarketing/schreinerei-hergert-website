# Cloudflare Worker source — already deployed, excluded so Pages ignores it
worker-src/

# Astro build output
dist/
.astro/

# Node
node_modules/

# Omelette design-tool sidecar (image slot state — not for production)
.image-slots.state.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
